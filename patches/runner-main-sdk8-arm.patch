diff --git a/src/Misc/externals.sh b/src/Misc/externals.sh
index b14d2b04..c8acec8a 100755
--- a/src/Misc/externals.sh
+++ b/src/Misc/externals.sh
@@ -4,6 +4,9 @@ PRECACHE=$2
 
 NODE_URL=https://nodejs.org/dist
 NODE_ALPINE_URL=https://github.com/actions/alpine_nodejs/releases/download
+# node22 is only consumed by the linux-arm (armv7l) package; keep it declared here,
+# away from the NODE20/NODE24 lines that upstream bumps every release.
+NODE22_VERSION="22.22.3"
 # When you update Node versions you must also create a new release of alpine_nodejs at that updated version.
 # Follow the instructions here: https://github.com/actions/alpine_nodejs?tab=readme-ov-file#getting-started
 NODE20_VERSION="20.20.2"
@@ -186,4 +189,5 @@ fi
 
 if [[ "$PACKAGERUNTIME" == "linux-arm" ]]; then
     acquireExternalTool "$NODE_URL/v${NODE20_VERSION}/node-v${NODE20_VERSION}-linux-armv7l.tar.gz" node20 fix_nested_dir
+    acquireExternalTool "$NODE_URL/v${NODE22_VERSION}/node-v${NODE22_VERSION}-linux-armv7l.tar.gz" node22 fix_nested_dir
 fi
diff --git a/src/Runner.Common/Constants.cs b/src/Runner.Common/Constants.cs
index 4acf4943..a2829810 100644
--- a/src/Runner.Common/Constants.cs
+++ b/src/Runner.Common/Constants.cs
@@ -197,6 +197,7 @@ namespace GitHub.Runner.Common
                 // Node versions
                 public static readonly string Node20 = "node20";
                 public static readonly string Node24 = "node24";
+                public static readonly string Node22 = "node22";
 
                 // Environment variables for controlling node version selection
                 public static readonly string ForceNode24Variable = "FORCE_JAVASCRIPT_ACTIONS_TO_NODE24";
diff --git a/src/Runner.Common/Runner.Common.csproj b/src/Runner.Common/Runner.Common.csproj
index 7f7e4106..e240f07d 100644
--- a/src/Runner.Common/Runner.Common.csproj
+++ b/src/Runner.Common/Runner.Common.csproj
@@ -1,7 +1,7 @@
 <Project Sdk="Microsoft.NET.Sdk">
 
   <PropertyGroup>
-    <TargetFramework>net8.0</TargetFramework>
+    <TargetFramework>net9.0</TargetFramework>
     <OutputType>Library</OutputType>
     <RuntimeIdentifiers>win-x64;win-x86;linux-x64;linux-arm64;linux-arm;osx-x64;osx-arm64;win-arm64</RuntimeIdentifiers>
     <TargetLatestRuntimePatch>true</TargetLatestRuntimePatch>
diff --git a/src/Runner.Common/Util/NodeUtil.cs b/src/Runner.Common/Util/NodeUtil.cs
index d87224f9..07d822fb 100644
--- a/src/Runner.Common/Util/NodeUtil.cs
+++ b/src/Runner.Common/Util/NodeUtil.cs
@@ -144,7 +144,7 @@ namespace GitHub.Runner.Common.Util
 
                 if (string.Equals(preferredVersion, Constants.Runner.NodeMigration.Node24, StringComparison.OrdinalIgnoreCase))
                 {
-                    return (Constants.Runner.NodeMigration.Node20, deprecationWarning);
+                    return (Constants.Runner.NodeMigration.Node22, deprecationWarning);
                 }
 
                 return (preferredVersion, deprecationWarning);
@@ -153,7 +153,7 @@ namespace GitHub.Runner.Common.Util
             // Legacy behavior: fall back to node20 if node24 was requested on ARM32
             if (string.Equals(preferredVersion, Constants.Runner.NodeMigration.Node24, StringComparison.OrdinalIgnoreCase))
             {
-                return (Constants.Runner.NodeMigration.Node20, "Node 24 is not supported on Linux ARM32 platforms. Falling back to Node 20.");
+                return (Constants.Runner.NodeMigration.Node22, "Node 24 is not supported on Linux ARM32 platforms. Falling back to Node 22.");
             }
 
             return (preferredVersion, null);
diff --git a/src/Runner.Listener/Runner.Listener.csproj b/src/Runner.Listener/Runner.Listener.csproj
index d2b1060d..d7119171 100644
--- a/src/Runner.Listener/Runner.Listener.csproj
+++ b/src/Runner.Listener/Runner.Listener.csproj
@@ -1,7 +1,7 @@
 <Project Sdk="Microsoft.NET.Sdk">
 
   <PropertyGroup>
-    <TargetFramework>net8.0</TargetFramework>
+    <TargetFramework>net9.0</TargetFramework>
     <OutputType>Exe</OutputType>
     <RuntimeIdentifiers>win-x64;win-x86;linux-x64;linux-arm64;linux-arm;osx-x64;osx-arm64;win-arm64</RuntimeIdentifiers>
     <SelfContained>true</SelfContained>
diff --git a/src/Runner.PluginHost/Runner.PluginHost.csproj b/src/Runner.PluginHost/Runner.PluginHost.csproj
index 81a8d2e4..49272a23 100644
--- a/src/Runner.PluginHost/Runner.PluginHost.csproj
+++ b/src/Runner.PluginHost/Runner.PluginHost.csproj
@@ -1,7 +1,7 @@
 ﻿<Project Sdk="Microsoft.NET.Sdk">
 
   <PropertyGroup>
-    <TargetFramework>net8.0</TargetFramework>
+    <TargetFramework>net9.0</TargetFramework>
     <OutputType>Exe</OutputType>
     <RuntimeIdentifiers>win-x64;win-x86;linux-x64;linux-arm64;linux-arm;osx-x64;osx-arm64;win-arm64</RuntimeIdentifiers>
     <SelfContained>true</SelfContained>
diff --git a/src/Runner.Plugins/Runner.Plugins.csproj b/src/Runner.Plugins/Runner.Plugins.csproj
index a786cf1c..60d7662b 100644
--- a/src/Runner.Plugins/Runner.Plugins.csproj
+++ b/src/Runner.Plugins/Runner.Plugins.csproj
@@ -1,7 +1,7 @@
 ﻿<Project Sdk="Microsoft.NET.Sdk">
 
   <PropertyGroup>
-    <TargetFramework>net8.0</TargetFramework>
+    <TargetFramework>net9.0</TargetFramework>
     <OutputType>Library</OutputType>
     <RuntimeIdentifiers>win-x64;win-x86;linux-x64;linux-arm64;linux-arm;osx-x64;osx-arm64;win-arm64</RuntimeIdentifiers>
     <SelfContained>true</SelfContained>
diff --git a/src/Runner.Sdk/Runner.Sdk.csproj b/src/Runner.Sdk/Runner.Sdk.csproj
index 906b78f6..0ec87c35 100644
--- a/src/Runner.Sdk/Runner.Sdk.csproj
+++ b/src/Runner.Sdk/Runner.Sdk.csproj
@@ -1,7 +1,7 @@
 ﻿<Project Sdk="Microsoft.NET.Sdk">
 
   <PropertyGroup>
-    <TargetFramework>net8.0</TargetFramework>
+    <TargetFramework>net9.0</TargetFramework>
     <OutputType>Library</OutputType>
     <RuntimeIdentifiers>win-x64;win-x86;linux-x64;linux-arm64;linux-arm;osx-x64;osx-arm64;win-arm64</RuntimeIdentifiers>
     <SelfContained>true</SelfContained>
diff --git a/src/Runner.Worker/Handlers/HandlerFactory.cs b/src/Runner.Worker/Handlers/HandlerFactory.cs
index 8044f091..1ff2a023 100644
--- a/src/Runner.Worker/Handlers/HandlerFactory.cs
+++ b/src/Runner.Worker/Handlers/HandlerFactory.cs
@@ -33,6 +33,12 @@ namespace GitHub.Runner.Worker.Handlers
                 string.Equals(finalNodeVersion, Constants.Runner.NodeMigration.Node20, StringComparison.OrdinalIgnoreCase);
         }
 
+        internal static bool ShouldTrackAsDeprecatedNode20(bool warnOnNode20, string finalNodeVersion)
+        {
+            return warnOnNode20 &&
+                string.Equals(finalNodeVersion, Constants.Runner.NodeMigration.Node20, StringComparison.OrdinalIgnoreCase);
+        }
+
         public IHandler Create(
             IExecutionContext executionContext,
             Pipelines.ActionStepDefinitionReference action,
@@ -122,7 +128,7 @@ namespace GitHub.Runner.Worker.Handlers
                             // Action is on node20 because ARM32 can't run node24
                             executionContext.Global.Arm32Node20Actions?.Add(actionName);
                         }
-                        else if (warnOnNode20)
+                        else if (ShouldTrackAsDeprecatedNode20(warnOnNode20, finalNodeVersion))
                         {
                             // Action is still running on node20 (general case)
                             executionContext.Global.DeprecatedNode20Actions?.Add(actionName);
diff --git a/src/Runner.Worker/Runner.Worker.csproj b/src/Runner.Worker/Runner.Worker.csproj
index 98717965..4bd28817 100644
--- a/src/Runner.Worker/Runner.Worker.csproj
+++ b/src/Runner.Worker/Runner.Worker.csproj
@@ -1,7 +1,7 @@
 <Project Sdk="Microsoft.NET.Sdk">
 
   <PropertyGroup>
-    <TargetFramework>net8.0</TargetFramework>
+    <TargetFramework>net9.0</TargetFramework>
     <OutputType>Exe</OutputType>
     <RuntimeIdentifiers>win-x64;win-x86;linux-x64;linux-arm64;linux-arm;osx-x64;osx-arm64;win-arm64</RuntimeIdentifiers>
     <SelfContained>true</SelfContained>
diff --git a/src/Sdk/Sdk.csproj b/src/Sdk/Sdk.csproj
index 5db8c10e..49bc0143 100644
--- a/src/Sdk/Sdk.csproj
+++ b/src/Sdk/Sdk.csproj
@@ -1,7 +1,7 @@
 <Project Sdk="Microsoft.NET.Sdk">
 
     <PropertyGroup>
-        <TargetFramework>net8.0</TargetFramework>
+        <TargetFramework>net9.0</TargetFramework>
         <OutputType>Library</OutputType>
         <RuntimeIdentifiers>win-x64;win-x86;linux-x64;linux-arm64;linux-arm;osx-x64;osx-arm64;win-arm64</RuntimeIdentifiers>
         <!-- <SelfContained>true</SelfContained> -->
diff --git a/src/Test/L0/Worker/HandlerFactoryL0.cs b/src/Test/L0/Worker/HandlerFactoryL0.cs
index e8ecdef3..ab106a46 100644
--- a/src/Test/L0/Worker/HandlerFactoryL0.cs
+++ b/src/Test/L0/Worker/HandlerFactoryL0.cs
@@ -869,5 +869,27 @@ namespace GitHub.Runner.Common.Tests.Worker
 
             Assert.Equal(expected, actual);
         }
+
+        [Theory]
+        [Trait("Level", "L0")]
+        [Trait("Category", "Worker")]
+        [InlineData(true, "node20", true)]
+        [InlineData(true, "node22", false)]
+        [InlineData(true, "node24", false)]
+        [InlineData(false, "node20", false)]
+        public void ShouldTrackAsDeprecatedNode20_OnlyTracksNode20(
+            bool warnOnNode20,
+            string finalNodeVersion,
+            bool expected)
+        {
+            // On Linux ARM32, node24-targeted actions resolve to node22 (not node20).
+            // The deprecated-node20 annotation must only apply to actions actually
+            // running on node20, otherwise node22-executed actions are mislabeled.
+            bool actual = HandlerFactory.ShouldTrackAsDeprecatedNode20(
+                warnOnNode20,
+                finalNodeVersion);
+
+            Assert.Equal(expected, actual);
+        }
     }
 }
diff --git a/src/Test/L0/Worker/StepHostNodeVersionL0.cs b/src/Test/L0/Worker/StepHostNodeVersionL0.cs
index 1c626c5d..68d04f5d 100644
--- a/src/Test/L0/Worker/StepHostNodeVersionL0.cs
+++ b/src/Test/L0/Worker/StepHostNodeVersionL0.cs
@@ -34,8 +34,8 @@ namespace GitHub.Runner.Common.Tests.Worker
 
             if (isArm32 && isLinux)
             {
-                // Should downgrade to node20 on ARM32 Linux
-                Assert.Equal("node20", nodeVersion);
+                // Should use node22 on ARM32 Linux (node24 is unavailable)
+                Assert.Equal("node22", nodeVersion);
                 Assert.NotNull(warningMessage);
                 Assert.Contains("Node 24 is not supported on Linux ARM32 platforms", warningMessage);
             }
@@ -74,7 +74,7 @@ namespace GitHub.Runner.Common.Tests.Worker
 
             if (isArm32 && isLinux)
             {
-                Assert.Equal("node20", nodeVersion);
+                Assert.Equal("node22", nodeVersion);
                 Assert.NotNull(warningMessage);
                 Assert.Contains("deprecated", warningMessage);
                 Assert.Contains("no longer be supported", warningMessage);
@@ -178,7 +178,7 @@ namespace GitHub.Runner.Common.Tests.Worker
 
             if (isArm32 && isLinux)
             {
-                Assert.Equal("node20", nodeVersion);
+                Assert.Equal("node22", nodeVersion);
                 Assert.NotNull(warningMessage);
                 Assert.Contains(customDate, warningMessage);
                 Assert.DoesNotContain(Constants.Runner.NodeMigration.Node20RemovalDate, warningMessage);
@@ -205,7 +205,7 @@ namespace GitHub.Runner.Common.Tests.Worker
 
             if (isArm32 && isLinux)
             {
-                Assert.Equal("node20", nodeVersion);
+                Assert.Equal("node22", nodeVersion);
                 Assert.NotNull(warningMessage);
                 Assert.Contains(Constants.Runner.NodeMigration.Node20RemovalDate, warningMessage);
             }
diff --git a/src/Test/Test.csproj b/src/Test/Test.csproj
index 37ffb5b2..d2a96caa 100644
--- a/src/Test/Test.csproj
+++ b/src/Test/Test.csproj
@@ -1,6 +1,6 @@
 <Project Sdk="Microsoft.NET.Sdk">
     <PropertyGroup>
-        <TargetFramework>net8.0</TargetFramework>
+        <TargetFramework>net9.0</TargetFramework>
         <RuntimeIdentifiers>win-x64;win-x86;linux-x64;linux-arm64;linux-arm;osx-x64;osx-arm64;win-arm64</RuntimeIdentifiers>
         <TargetLatestRuntimePatch>true</TargetLatestRuntimePatch>
         <NoWarn>NU1701;NU1603;NU1603;xUnit2013;SYSLIB0050;SYSLIB0051</NoWarn>
diff --git a/src/dev.sh b/src/dev.sh
index 88a64285..e12b1f2c 100755
--- a/src/dev.sh
+++ b/src/dev.sh
@@ -17,7 +17,7 @@ LAYOUT_DIR="$SCRIPT_DIR/../_layout"
 DOWNLOAD_DIR="$SCRIPT_DIR/../_downloads/netcore2x"
 PACKAGE_DIR="$SCRIPT_DIR/../_package"
 DOTNETSDK_ROOT="$SCRIPT_DIR/../_dotnetsdk"
-DOTNETSDK_VERSION="8.0.424"
+DOTNETSDK_VERSION="9.0.318"
 DOTNETSDK_INSTALLDIR="$DOTNETSDK_ROOT/$DOTNETSDK_VERSION"
 RUNNER_VERSION=$(cat runnerversion)
 
diff --git a/src/global.json b/src/global.json
index 9af44fb9..70cfe5c2 100644
--- a/src/global.json
+++ b/src/global.json
@@ -1,5 +1,5 @@
 {
   "sdk": {
-    "version": "8.0.424"
+    "version": "9.0.318"
   }
 }
# From upstream commit: 759385a3510197a58b5c08dc1f373b74b9f4643b
